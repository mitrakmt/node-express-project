module.exports = {
  albumsController: require('./albums.controller').albumsController,
  postsController: require('./posts.controller').postsController,
  usersController: require('./users.controller').usersController
}
